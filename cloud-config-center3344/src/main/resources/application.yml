server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dyeingseven/springcloud-config.git
          username: dyeingseven
          password: RanQi594250
          strict-host-key-checking: false
          search-paths: springcloud-config
      label: master
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka
